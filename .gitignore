# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.venv/
venv/
env/

# ── OS / 에디터 ──
.DS_Store
Thumbs.db
*.swp

# ── Claude Code 개인 설정 (공유 대상 아님) ──
.claude/settings.local.json

# ── 인코딩 변환 전 CP949 원본 백업 (재현에 불필요·중복) ──
# 원본을 저장소에 함께 두고 싶으면 아래 한 줄을 지우세요.
data/_backup_cp949/
